import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Alhaq Learning Center",
  description: "Alhaq Learning Center adalah pusat pendidikan dan bimbingan belajar yang beralamat di Bata Merah, Purwokerto. Kami menyediakan berbagai program edukasi untuk kelompok usia dini hingga dewasa, dengan fokus pada pendidikan umum dan nilai-nilai spiritual Islam.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
